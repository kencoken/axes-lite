[supervisord]
logfile=supervisor.log ; (main log file;default $CWD/supervisord.log)
pidfile=supervisor.pid ; (supervisord pidfile;default supervisord.pid)
childlogdir=logs ; ('AUTO' child log dir, default $TEMP)

[unix_http_server]
file=supervisord.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

[supervisorctl]
serverurl=unix://supervisord.sock ; use a unix:// URL  for a unix socket

[program:cpuvisor-srv]
command=start.sh
autostart=false
directory=<CPUVISOR-SRV>

[program:limas]
command=<LIMAS>/bin/limas webstart <LIMAS_PORT> <LIMAS_CONF>
autostart=false
directory=<LIMAS>/bin

[program:imsearch-tools]
command=python imsearch_http_service.py <IMSEARCH-TOOLS_PORT>
autostart=false
directory=<IMSEARCH-TOOLS>

[program:axes-home]
command=<AXES-HOME>/start.sh
autostart=false
directory=<AXES-HOME>

# [program:mongodb]
# command=bin/mongod --dbpath <INDEX_PATH>/mongodb
# autostart=false
# directory=<MONGODB>
#
[program:nginx]
command=nginx -c <NGINX>/conf/nginx.conf
autostart=false
directory=<NGINX>/sbin
